import Contact from '@/components/pages/ContactPage/Contact'

const ContactLayout = ({ children }: { children: React.ReactNode }) => {
  return <Contact children={children} />
}
export default ContactLayout